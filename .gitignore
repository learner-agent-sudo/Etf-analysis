# Pasted/raw source documents — keep the repo about analysis, not bulk data.
# Remove these lines if you DO want to version specific source files.
*.pdf
*.csv
*.xlsx
*.xls
data/raw/

# Scratch / personal
scratch/
*.tmp
.DS_Store

# Node (app uses zero deps; these only appear from stray installs)
node_modules/
package-lock.json
package.json

# Python (if/when automation is added)
__pycache__/
*.pyc
.venv/
venv/
.env
